const USERS = require('./users');
const PATTERNS = require('./patterns');
const { RECORDS, RECORD_STATES, SIMULATION_STATES } = require('./records');
const LABELS = require('./labels');

module.exports = {
    USERS,
    PATTERNS,
    RECORDS,
    LABELS,
    RECORD_STATES,
    SIMULATION_STATES
}